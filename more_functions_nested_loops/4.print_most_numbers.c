#include "main.h"
/**
 * print_most_numbers - prints most numbers.
 *
 * Return: Always 0.
 */
void print_most_numbers(void)
{
	char i;

	for (i = '0'; i <= '9'; i++)
	{
		if ((n == '2') || (n == '4'))
		{
			continue;
		}
		else
		{
			_putchar(n);
		}
	}
	_putchar(10);

}
